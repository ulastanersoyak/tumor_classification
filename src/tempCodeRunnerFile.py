        # for i in range(len(convnet2map)):
        #     axs[1, i].imshow(convnet2map[i], cmap='gray')
        #     axs[1, i].set_title('Convnet2 Activation Map {}'.format(i+1))
        #     axs[1, i].axis('off')

        # # Plot the activation maps for convnet3map
        # for i in range(len(convnet3map)):
        #     axs[2, i].imshow(convnet3map[i], cmap='gray')
        #     axs[2, i].set_title('Convnet3 Activation Map {}'.format(i+1))
        #     axs[2, i].axis('off')

        # # Plot the activation maps for convnet4map
        # for i in range(len(convnet4map)):
        #     axs[3, i].imshow(convnet4map[i], cmap='gray')
        #     axs[3, i].set_title('Convnet4 Activation Map {}'.format(i+1))
        #     axs[3, i].axis('off')